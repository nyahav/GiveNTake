import useStreets from "../../../hooks/useStreets";

const StreetSelector = (city) => {

    const streets = useStreets(city);
    
    // console.log(streets)

    return <></>;
};

export default StreetSelector;